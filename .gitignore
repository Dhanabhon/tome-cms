node_modules/
dist/
/dist-updater/
.astro/

# Local environment
.env
.env.*
!.env.example
*.license
*.lic

# Local Compose state and overrides
minio-data/
postgres-data/
seaweedfs-data/
compose.override.yml
compose.override.yaml
docker-compose.override.yml
docker-compose.override.yaml

# Local AI tools, skills, and generated previews
/.agent/
/.agents/
/.claude/
/.codex/
/.cursor/
/.gemini/
/.hallmark/
/.impeccable/
/.superpowers/
/.zvec-grep/
/docs/superpowers/
/.worktrees/
test-results/
# Screenshots and reports from visual-check runs; the source document is tracked.
docs/*.visual-check.*

# Design source sheets. The whole folder, so no naming variation can slip past --
# an anchored pattern is how tomecms-brand-kit-v5.png got committed in the first place.
docs/brand/

# DB-IP Lite, downloaded by the release workflow (or by hand for local Stats countries).
/data/geoip/

# OS and logs
.DS_Store
*.log

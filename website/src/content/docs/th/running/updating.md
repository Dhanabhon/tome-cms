---
title: การอัปเดต
description: อัปเกรดเว็บที่ติดตั้งเป็น 0.x บนเครื่องเดิมด้วยสคริปต์ deploy และดูว่าแต่ละเวอร์ชันก่อนหน้ามี migration ของฐานข้อมูลค้างอยู่กี่ตัว
sidebar:
  order: 3
---

TomeCMS ตอนนี้ยังเป็นรุ่นพรีวิวก่อน 1.0 เว็บที่ติดตั้งเป็น 0.x อัปเกรดได้บนเครื่องเดิม โดยดึงโค้ดที่ใหม่กว่ามาแล้วรันสคริปต์ deploy ตัวเดียวกับที่ใช้ติดตั้ง

หน้าแอดมินยังติดตั้งอัปเดตเองไม่ได้ ที่เมนู "ระบบ" จะแสดงเวอร์ชันของคุณไว้ข้าง "เวอร์ชันที่ติดตั้งอยู่:" และตรวจกับ repository ทางการบน GitHub ได้ว่ามีรุ่น stable ที่ใหม่กว่าหรือยัง ในการติดตั้งตอนนี้ หน้านั้นจะขึ้นว่า "ใช้การอัปเดตแบบจัดการให้ไม่ได้" และ "โหมดการอัปเดต:" เป็น `check-only`

## อัปเกรดเว็บที่เป็น 0.x

1. หยุดแอป แล้วสำรอง PostgreSQL และ bucket ของมีเดียไว้พร้อมกัน และตรวจชุดสำรองตามหน้า[สำรองและกู้คืนข้อมูล](/tome-cms/th/running/backups/) จากนั้นปล่อยแอปหยุดไว้อย่างนั้น เพื่อไม่ให้มีข้อมูลใหม่ที่ชุดสำรองไม่มี
2. อ่าน release notes ของทุกเวอร์ชันที่ใหม่กว่าเวอร์ชันของคุณ ไฟล์อยู่ใน `docs/releases/` ของ repository เวอร์ชันละหนึ่งไฟล์ และแต่ละไฟล์บอกว่าการอัปเกรดเวอร์ชันนั้นต้องทำอะไรบ้าง
3. ในโฟลเดอร์โค้ด ดึงโค้ดใหม่แล้วรันสคริปต์

   ```sh
   git pull
   ./scripts/deploy-vps.sh
   ```

4. ตรวจว่าแอปพร้อมแล้ว แบบเดียวกับตอน[ติดตั้งครั้งแรก](/tome-cms/th/start/install/)

   ```sh
   curl -s http://127.0.0.1:4321/health/ready
   ```

สคริปต์ใช้ `.env.local` เดิมโดยไม่แก้อะไร จากนั้น build image ใหม่ของแอป รัน migration ที่ค้างอยู่ในคอนเทนเนอร์ที่ใช้ครั้งเดียวแล้วทิ้ง แล้วจึงเปิดคอนเทนเนอร์แอปตัวใหม่ เว็บจะใช้ไม่ได้ตั้งแต่ตอนที่คุณหยุดแอปไปจนถึงตอนที่คอนเทนเนอร์ตัวใหม่พร้อม

เว็บที่ติดตั้งจาก 0.2.0 ยังไม่มีค่าบางตัวที่สคริปต์รุ่นใหม่เขียน สคริปต์จึงหยุดพร้อมข้อความ `Existing .env.local needs updates; rerun with --force to merge values while preserving secrets.` ให้รัน `./scripts/deploy-vps.sh --force` สคริปต์จะเพิ่มค่าที่ขาดและคงค่าลับเดิมไว้

## ปรับฐานข้อมูลด้วย migration

เวอร์ชันใหม่อาจเปลี่ยนโครงสร้างตารางในฐานข้อมูล การเปลี่ยนแต่ละครั้งเรียกว่า migration และคำสั่งนี้จะรันทุกตัวที่ฐานข้อมูลยังไม่มี

```sh
npm run db:migrate
```

สคริปต์ deploy รันคำสั่งนี้ให้ในคอนเทนเนอร์แอปที่ใช้ครั้งเดียวแล้วทิ้ง ในการติดตั้งตอนนี้จึงไม่ต้องรันเอง ถ้าจะรันเองในโฟลเดอร์โค้ด ให้รัน `npm ci` ในโฟลเดอร์นั้นหนึ่งครั้งก่อน คำสั่งนี้อ่านข้อมูลการเชื่อมต่อจาก `.env.local`

คำสั่งจะพิมพ์ `<name>: Success` หนึ่งบรรทัดต่อ migration ที่รันสำเร็จ หรือพิมพ์ `Up to date` ถ้าไม่มีตัวไหนค้างอยู่ migration ที่ค้างอยู่ทั้งหมดรันใน transaction เดียวของ PostgreSQL ถ้ามีตัวใดล้มเหลว คำสั่งจะพิมพ์ `Migration failed` และจะไม่มี migration ตัวไหนมีผลเลย ฐานข้อมูลจะอยู่ในสภาพเดิม

TomeCMS ไม่มีคำสั่งย้อน migration ถ้าจะกลับไปใช้เวอร์ชันเก่า ให้กู้คืนจากชุดสำรองที่ทำไว้ก่อนอัปเกรด

## จำนวน migration ที่ค้างอยู่

ดูว่าเว็บของคุณติดตั้งจากเวอร์ชันไหน หรืออัปเกรดครั้งล่าสุดเป็นเวอร์ชันไหน แล้วนับจากเวอร์ชันนั้น

| ติดตั้งจากเวอร์ชัน | ค้างอยู่ | ตัวไหนบ้าง |
| --- | --- | --- |
| 0.11.0 | 0 | ไม่มี |
| 0.10.0 | 2 | `024_site_maintenance` และ `025_content_stats` |
| 0.9.0 หรือ 0.8.0 | 3 | สองตัวข้างบน และ `023_home_slides` |
| 0.7.0 หรือ 0.6.0 | 5 | สามตัวข้างบน `021_media_documents` และ `022_navigation_new_tab` |
| 0.5.0 หรือ 0.4.0 | 6 | ห้าตัวข้างบน และ `020_site_brand` |
| 0.3.0 | 9 | หกตัวข้างบน `017_scheduled_publishing`, `018_thai_slugs` และ `019_content_redirects` |
| 0.2.0 | 18 | เก้าตัวข้างบน และ `008_update_rate_limit_actions` ไปจนถึง `016_theme_settings` |

ตัวเลขเหล่านี้นับตาม release notes ของแต่ละเวอร์ชัน ถ้าติดตั้งจากโค้ดที่อยู่ระหว่างสองรุ่น หรือจากโค้ดก่อน 0.2.0 จำนวนที่ค้างอาจไม่ตรงกับตาราง ประกาศในหน้าแอดมินจะบอกชื่อที่ค้างอยู่จริง

## ประกาศในหน้าแอดมิน

เมื่อโค้ดต้องการ migration ที่ฐานข้อมูลยังไม่มี ทุกหน้าของแอดมินจะขึ้นประกาศ "ฐานข้อมูลตามรุ่นนี้ไม่ทัน" ไว้ด้านบน ตามด้วย "ที่ยังค้างอยู่:" และชื่อ migration ที่ค้าง พร้อมเตือนว่าการบันทึกเนื้อหาจะล้มเหลวจนกว่าจะรัน `npm run db:migrate` หน้าแอดมินถามฐานข้อมูลใหม่ทุกครั้งที่เปิดหน้า เมื่อรัน migration เสร็จแล้ว ประกาศจึงหายไปตั้งแต่หน้าถัดไปที่คุณเปิด

ระหว่างนั้น `/health/ready` จะรายงาน `"migrations":"pending"` และ `"status":"not-ready"`

## สิ่งที่จะเปลี่ยนใน 1.0.0

:::caution[ยังไม่เปิดให้ใช้]
ตัวอัปเดตแบบ managed มาพร้อมการติดตั้งแบบ managed ใน 1.0.0 ซึ่งยังไม่เปิดให้ใช้ ทุกข้อด้านล่างยังใช้กับการติดตั้งตอนนี้ไม่ได้
:::

- การติดตั้งแบบ managed มี service ของ systemd แยกอีกตัวชื่อ `tomecms-updater` ซึ่งทำให้เจ้าของเว็บติดตั้งอัปเดตที่ตรวจสอบแล้วได้จากเมนู "ระบบ" ในหน้าแอดมิน เริ่มจาก 1.0.0 ไป 1.0.1
- ก่อนอัปเดตทุกครั้ง ตัวอัปเดตจะสำรอง PostgreSQL และ bucket เต็มชุดไว้ที่ `/var/backups/tome-cms/` แล้วจึงรัน migration ตัวอัปเดตไม่ลบชุดสำรองเก่าเลย จึงต้องเผื่อพื้นที่ดิสก์ไว้ และคัดลอกชุดสำรองออกไปเก็บนอกเซิร์ฟเวอร์เอง
- ถ้าอัปเดตล้มเหลวหลังจากเริ่มรัน migration ไปแล้ว ตัวอัปเดตจะกลับไปใช้แอปตัวเดิมก็ต่อเมื่อ release ใหม่ระบุไว้ว่าเวอร์ชันเดิมใช้กับฐานข้อมูลใหม่ได้ ถ้าไม่ได้ระบุไว้ การอัปเดตจะหยุดรอให้ผู้ดูแลเซิร์ฟเวอร์จัดการ ส่วนการกู้ฐานข้อมูลและไฟล์คืนจากชุดสำรองต้องทำเองทุกครั้ง
- ไม่มีการอัปเดตอัตโนมัติ และไม่มีช่องทางรุ่น beta ส่วน service ของตัวอัปเดตเอง รวมถึง image ของ PostgreSQL และ SeaweedFS ต้องอัปเกรดด้วยมือ

การติดตั้ง 0.x เปลี่ยนเป็นแบบ managed บนเครื่องเดิมไม่ได้ การย้ายไป 1.0.0 ต้องใช้เซิร์ฟเวอร์เครื่องใหม่ ตามที่อธิบายไว้ในหน้า[ติดตั้งบน VPS](/tome-cms/th/start/install/)

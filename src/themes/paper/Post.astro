---
import './theme.css';
import PostArticle from './parts/PostArticle.astro';
import type { ThemePostProps } from '../contract';

interface Props extends ThemePostProps {}

const { categories, post, preview, profile, settings } = Astro.props;
---

<PostArticle categories={categories} post={post} preview={preview} profile={profile} settings={settings} />
